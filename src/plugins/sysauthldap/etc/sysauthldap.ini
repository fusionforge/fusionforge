[sysauthldap]

; plugin_status is a string.
; valid means : production ready.
; Any other strings means it's under work or broken and plugin
; is available in installation_environment = development only.
plugin_status = valid

enabled = no

ldap_host = $core/web_host
ldap_port = 389
ldap_version = 3
base_dn = fromhost:$core/web_host
bind_dn = "cn=admin,$sysauthldap/base_dn"
